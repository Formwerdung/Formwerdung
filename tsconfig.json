{
  "compilerOptions": {
    // Target latest version of ECMAScript.
    "target": "esnext",
    // Search under node_modules for non-relative imports.
    "moduleResolution": "node",
    // Process & infer types from .js files.
    "allowJs": true,
    // Don't emit; allow Babel to transform files.
    "noEmit": true,
    // Disable strictest settings like strictNullChecks & noImplicitAny.
    "strict": false,
    // Disallow features that require cross-file information for emit.
    "isolatedModules": true,
    // Import non-ES modules as default imports.
    "esModuleInterop": true,
    // Declare that we are using jsx and that it is of the react variety.
    "jsx": "react",
    // Disable error reporting to avoid false positive on Tailwinds @apply directive
    "plugins": [
      {
        "name": "typescript-styled-plugin",
        "validate": false,
        "tags": [
          "styled",
          "css",
        ]
      }
    ]
  }
}
